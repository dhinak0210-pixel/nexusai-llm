# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Node
node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment files (secrets — never commit!)
.env
.env.local
.env.*.local

# Python
__pycache__
*.pyc
*.pyo
*.pyd
.Python
*.egg-info
.eggs

# Heavy base model weights (NOT LoRA adapters — those are small and needed for deployment)
backend/fine_tuned_lora_old/

# Large standalone model weight files (base model downloads are cached via HuggingFace)
*.bin
*.pt
*.pth

# Allow LoRA adapter files (small, needed for Space deployment)
# backend/fine_tuned_lora/adapter_model.safetensors is ~49MB — acceptable
!backend/fine_tuned_lora/adapter_model.safetensors
!backend/fine_tuned_lora/adapter_config.json
!backend/fine_tuned_lora/tokenizer.json
!backend/fine_tuned_lora/tokenizer_config.json
!backend/fine_tuned_lora/special_tokens_map.json
!backend/fine_tuned_lora/chat_template.jinja

# Misc
backend/server.log
frontend.log
setup_model.log
Dhinakaran_Resume.pdf
test_upload.txt
